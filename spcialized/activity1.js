let userNum
let curPower

curPower = 2
userNum = window.prompt("25")

if (userNum ==1)
   console. log (curPower)
console. log ("\n")
console. log ("curPower = curPower * 2")
console. log ("userNum = Get next input")
console. log ("Done.")